describe("units", () => {
  describe("red", () => {
    describe("Bloodrage Ogre", () => {
      it.skip("returns to owner's hand AFTER draw/discard");
    });

    describe("Captured Bugblatter", () => {
      it.skip("triggers its ability own its own death");
    });
  });
});
