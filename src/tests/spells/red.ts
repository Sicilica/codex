import { expect } from "chai";

describe("spells", () => {
  describe("red", () => {
    describe("Pillage", () => {
      it("can't steal more gold than the target has", () => {
        expect(true).to.equal(false);
      });
    });
  });
});
