describe("basic", () => {
  describe("prompts", () => {
    it.skip("blocks gameplay until resolved");

    it.skip("can be resolved in any order");

    it.skip("dequeues events that become obsolete");
  });
});
